<!-- 地图 -->
<view class="map">
  <map id="map" class="map" markers="{{markers}}" longitude="{{longitude}}" latitude="{{latitude}}"  polyline="{{polyline}}" scale="{{11}}" show-location="{{true}}" enable-satellite="{{false}}"></map>
</view>